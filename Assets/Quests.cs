using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Quests : MonoBehaviour
{

    // TO DO:
    void Start()
    {
        string[] missions;
        string[] rewards;
        int mission = (int)Random.Range(0f, 2f);
        int reward = (int)Random.Range(0f, 2f);

    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
